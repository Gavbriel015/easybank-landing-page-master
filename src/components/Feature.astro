---

const { title, image, text } = Astro.props;

---

<div class="flex flex-col justify-center items-center px-4 gap-4">
    <img class="w-16" src={image} alt="">
    <h2 class=" text-darkBlue font-bold">{title}</h2>
    <h3 class="text-gray-500">{text}</h3>
</div>