---
interface Props {
	title: string;
	image: string;
	text: string;
}

const { title, image, text } = Astro.props;
---

<div>
    <img src={image} alt="">
    <h2>{title}</h2>
    <p>{text}</p>
</div>